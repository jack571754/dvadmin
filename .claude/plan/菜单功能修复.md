# 菜单功能修复 - 执行计划

## 问题
图书管理、博客管理、定时任务功能在前端不显示。

## 根因
- 图书/博客：后端路由和前端页面都存在，但菜单数据未初始化到数据库
- 定时任务：django-celery-beat 已安装，但无前端页面和菜单

## 执行步骤

| # | 步骤 | 状态 |
|---|------|------|
| 1 | 运行 `init_book_menu` 初始化图书菜单 | ✅ |
| 2 | 运行 `init_blog_menu` 初始化博客菜单 | ✅ |
| 3 | 创建 `celery_task.py` 后端 ViewSet | ✅ |
| 4 | 注册路由 (celery_task/interval_schedule/crontab_schedule) | ✅ |
| 5 | 创建前端 celeryTask 页面 (api.ts + crud.tsx + index.vue) | ✅ |
| 6 | 创建 `init_celery_task_menu` 命令并运行 | ✅ |

## 新增文件
- `backend/dvadmin/system/views/celery_task.py` - 定时任务 ViewSet
- `backend/dvadmin/system/management/commands/init_celery_task_menu.py` - 菜单初始化
- `web/src/views/system/celeryTask/api.ts` - 前端 API
- `web/src/views/system/celeryTask/crud.tsx` - CRUD 配置
- `web/src/views/system/celeryTask/index.vue` - 页面组件

## 修改文件
- `backend/dvadmin/system/urls.py` - 新增路由注册
