# 定时任务配置页面体验优化

## 任务目标
提升 `/celeryTask/task` 定时任务配置页面的用户体验，使配置更加清晰直观。

## 方案
Tabs 分组 + 调度类型单选互斥 + 中文化 + 行内开关

## 改动文件
- `web/src/views/system/celeryTask/crud.tsx` (唯一改动文件)

## 执行步骤
1. 表单 Tabs 分组（基本信息/调度配置/高级设置）
2. 新增调度类型单选虚拟字段 schedule_type，控制 interval/crontab 互斥
3. Crontab 选项中文化
4. 间隔调度 period 中文化
5. 增强 helper 提示
6. 启用状态改为行内开关
7. 新增 start_time 和 expires 字段

## 状态
- [x] 计划已批准
- [x] 执行完成
- [x] 优化审查通过
- [x] 用户验收通过
