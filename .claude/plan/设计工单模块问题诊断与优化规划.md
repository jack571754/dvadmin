# 设计工单模块问题诊断与优化规划

> 生成时间：2026-06-12
> 分析范围：`backend/dvadmin/design_order/` + `web/src/views/design_order/` + `web/src/api/design_order/`

---

## 一、问题全景图

按严重程度分为 4 级：**P0（致命/数据风险）→ P1（严重/功能缺陷）→ P2（重要/体验问题）→ P3（改进/代码质量）**

```
发现总计：42 项
├── P0 致命：6 项（运行时崩溃 + 数据安全）
├── P1 严重：14 项（功能缺陷 + 架构问题）
├── P2 重要：12 项（性能 + 体验 + 一致性）
└── P3 改进：10 项（代码质量 + 规范）
```

---

## 二、P0 致命问题（必须立即修复）

### 2.1 前端运行时崩溃

| # | 问题 | 文件:行号 | 影响 |
|---|------|-----------|------|
| 1 | **未声明变量** `giftEditRow`/`giftEditCol`/`giftList`/`giftModalVisible` | `index.vue:2077-2081` | `openGiftModal` 调用时立即抛出 `ReferenceError`，赠品配置功能完全不可用 |
| 2 | **未定义函数** `formatGiftsToText` | `index.vue:1617` | `syncFormToUniver` 执行时崩溃，表格数据同步失败 |
| 3 | **函数名使用中文** `submitWorkbookData正式` | `index.vue:2245` | 部分构建工具/Linter 可能报错，不规范且不可维护 |

### 2.2 后端数据安全

| # | 问题 | 文件:行号 | 影响 |
|---|------|-----------|------|
| 4 | **SaveProductSpecView 无事务保护** | `views.py:238-383` | 先 `delete()` 全部明细再逐条 `create()`，中途出错导致数据永久丢失 |
| 5 | **product_code 唯一约束冲突** | `views.py:358` | `f"SP{card_index+1:04d}"` 是纯序号，多次保存不同提报时重复触发 `IntegrityError`，异常被 `try/except` 吞掉，用户无感知 |
| 6 | **SaveProductSpecView 无速率限制** | `views.py:176` | 涉及大量数据库写入（删除+批量创建+产品档案同步），无防护可被滥用造成 DoS |

**修复方案：**

```
#1-3 前端崩溃修复：
  - 声明缺失的 ref 变量或重构赠品弹窗逻辑
  - 实现 formatGiftsToText 或替换为已有的 formatTiersToText
  - 重命名 submitWorkbookData正式 → submitWorkbookData

#4 事务保护：
  @transaction.atomic
  def post(self, request):
      ...
      ProductSpec.objects.filter(submission=submission).delete()
      for prod in products:
          ProductSpec.objects.create(...)
      # 任何异常自动回滚

#5 product_code 冲突：
  - 方案A：使用 UUID 或时间戳前缀代替纯序号
  - 方案B：跳过自动创建 ProductArchive，仅关联已存在的档案
  - 方案C：添加 submission_id 前缀，如 f"SP{submission.id}-{card_index:04d}"

#6 速率限制：
  - 使用 django-rest-framework 的 throttle_classes
  - 或在 settings.py 中配置 DEFAULT_THROTTLE_RATES
```

---

## 三、P1 严重问题（影响核心功能）

### 3.1 数据模型设计缺陷

| # | 问题 | 文件:行号 | 说明 |
|---|------|-----------|------|
| 7 | **ProductSpec.price 为 CharField** | `models.py:164` | 价格应为 DecimalField，与 ProductArchive.retail_price(DecimalField) 类型不一致，无法数值计算 |
| 8 | **start_date/end_date 为 CharField** | `models.py:165-166` | 无法数据库级别日期范围查询、排序、约束 |
| 9 | **ProductSpecSnapshot 死模型** | `models.py:176-187` | 定义+迁移均已创建，但无任何视图/序列化器/URL引用，快照实际存在 Submission.snapshot_data |
| 10 | **缺少 (submission, card_index) 唯一约束** | `models.py:142-146` | 同一提报下可存在重复 card_index 的明细记录 |
| 11 | **ProductArchive 字段语义重叠** | `models.py:50,54,74` | product_category / product_classification / category 三字段界限不清 |

### 3.2 API 设计缺陷

| # | 问题 | 文件:行号 | 说明 |
|---|------|-----------|------|
| 12 | **SaveProductSpecView 权限过弱** | `views.py:180` | 仅 `IsAuthenticated`，绕过了菜单按钮级权限控制；LoadProductSpecView 同样问题 |
| 13 | **URL 路由违反 RESTful** | `urls.py:16-18` | save_spec/load_spec 挂在 product_archives/ 下，操作的实际资源是 ProductSpecSubmission |
| 14 | **copy 操作无事务** | `views.py:82-110` | 先创建 Submission 再 bulk_create，后者失败会留下空记录 |

### 3.3 前端功能缺陷

| # | 问题 | 文件:行号 | 说明 |
|---|------|-----------|------|
| 15 | **表单验证完全缺失** | `index.vue:1087-1099` | 保存前无任何验证，必填字段可为空直接提交 |
| 16 | **全局 click 监听器未清理（内存泄漏）** | `index.vue:2486-2496` | `onBeforeUnmount` 中缺少对应 `removeEventListener` |
| 17 | **v-html 存在 XSS 风险** | `ProductSelectDialog.vue:36` | `highlightText` 未对原始 text 做 HTML 转义 |
| 18 | **字典接口绕过数据权限** | `views.py:41` | 使用 `self.queryset` 而非 `self.filter_queryset(self.queryset)` |

### 3.4 业务逻辑缺陷

| # | 问题 | 文件:行号 | 说明 |
|---|------|-----------|------|
| 19 | **字段遮罩依赖 `'***'` 字符串约定** | `views.py:248-310` | 用户真实数据若恰好是 `***` 会被错误替换 |
| 20 | **product_count 不同步** | `models.py:124` | 通过 ViewSet 直接 CRUD ProductSpec 时不更新 Submission.product_count |

---

## 四、P2 重要问题（性能/体验/一致性）

### 4.1 性能问题

| # | 问题 | 文件:行号 | 说明 |
|---|------|-----------|------|
| 21 | **SaveProductSpecView N+1 查询** | `views.py:244-383` | N 个产品 ~6N 次数据库操作，应批量查询+批量创建 |
| 22 | **ProductSpecViewSet 缺少 select_related** | `views.py:67` | 每条明细额外查询 submission + product 两个 FK |
| 23 | **ProductArchive 缺少数据库索引** | `models.py` | brand、product_name、nickname 频繁查询无索引 |
| 24 | **前端 O(n²) 搜索无防抖** | `index.vue:1236-1360` | 4 处搜索输入无防抖，产品数据增长后输入卡顿 |
| 25 | **getValue 函数重复定义 8 次** | `index.vue` 多处 | 相同逻辑在组件内重复 8 次 |

### 4.2 体验问题

| # | 问题 | 文件:行号 | 说明 |
|---|------|-----------|------|
| 26 | **日期使用纯文本输入** | `index.vue:128,193` | 应使用 `el-date-picker` 替代 `<input type="text">` |
| 27 | **错误处理不完善** | `index.vue:2448-2450` | API 失败仅 `console.warn`，用户无感知看到空白表格 |
| 28 | **缺少 loading 状态** | `index.vue` | 用 `ElMessage.info` 代替了真正的 loading |
| 29 | **产品数据库硬编码前端** | `constants.ts:1-105` | `PRODUCT_DATABASE` 包含 5 个产品完整信息，日期已过时 |
| 30 | **迁移逻辑在前端** | `index.vue:429-543` | `migrateSnapshotTo15Rows` 应在后端处理 |
| 31 | **默认值硬编码重复 7+ 处** | `index.vue` 多处 | 品牌名、档位值、日期等硬编码散布各处 |

### 4.3 一致性问题

| # | 问题 | 文件:行号 | 说明 |
|---|------|-----------|------|
| 32 | **两套遮罩机制不一致** | `views.py:38-60` vs `FieldPermissionMixin` | dict 接口手动遮罩，list/retrieve 由框架处理，逻辑不同步 |

---

## 五、P3 改进建议（代码质量/规范）

| # | 问题 | 说明 |
|---|------|------|
| 33 | **3109 行巨型组件** | `index.vue` 应拆分为 5+ 个 composable + 子组件 |
| 34 | **全量硬编码中文** | 未使用 vue-i18n，涉及所有文件 |
| 35 | **核心对象均为 any 类型** | univerAPI/workbook/sheet 丧失类型保护 |
| 36 | **ProductInfo 与 DBProductItem 类型重复** | 应合并或用 type alias |
| 37 | **GiftConfigModal 死代码** | 完整实现但从未被 import 使用 |
| 38 | **Admin 仅注册 ProductArchive** | 其他 3 个模型无法在 Django Admin 管理 |
| 39 | **序列化器无验证** | 三个序列化器均为 `fields = "__all__"` 最简形式 |
| 40 | **sale_status 与 product_status 值冲突** | 都含 'on_sale'，语义不同 |
| 41 | **merge_snapshots 直接修改输入参数** | 副作用影响 request.data |
| 42 | **ErrorResponse 未设置 HTTP 状态码** | 客户端收到 200 但 body code=400 |

---

## 六、优化路线图

### 第一阶段：紧急修复（1-2 天）— P0 全部 + P1 关键项

```
□ 修复前端运行时崩溃（#1-3）
□ 给 SaveProductSpecView 加 transaction.atomic（#4）
□ 修复 product_code 唯一约束冲突（#5）
□ 添加速率限制（#6）
□ 修复全局事件监听器内存泄漏（#16）
□ 修复 v-html XSS 风险（#17）
□ 修复字典接口绕过数据权限（#18）
```

### 第二阶段：数据模型优化（2-3 天）— P1 模型问题

```
□ ProductSpec.price: CharField → DecimalField（#7）
□ ProductSpec.start_date/end_date: CharField → DateField（#8）
□ 删除或激活 ProductSpecSnapshot 模型（#9）
□ 添加 unique_together = [('submission', 'card_index')]（#10）
□ 合并/清理 ProductArchive 重叠字段（#11）
□ SaveProductSpecView/LoadProductSpecView 改用 CustomPermission（#12）
□ URL 路由调整到 product_spec_submissions/ 下（#13）
□ copy 操作添加 transaction.atomic（#14）
□ 前端添加表单验证（#15）
□ 改进字段遮罩机制（#19）
□ ProductSpecViewSet 添加 filter_fields/select_related（#20, #22）
```

### 第三阶段：性能与体验优化（3-5 天）— P2

```
□ SaveProductSpecView 批量查询优化（#21）
□ 添加数据库索引（#23）
□ 搜索输入防抖 + 性能优化（#24）
□ 提取 getValue 为公共函数（#25）
□ 日期输入改用 el-date-picker（#26）
□ 统一错误处理与 loading 状态（#27-28）
□ 删除 PRODUCT_DATABASE 硬编码，统一从 API 获取（#29）
□ 迁移逻辑移至后端（#30）
□ 默认值提取为常量（#31）
□ 统一遮罩机制（#32）
```

### 第四阶段：架构重构（5-7 天）— P3 + 长期优化

```
□ index.vue 拆分为 composable + 子组件（#33）
□ 接入 vue-i18n（#34）
□ Univer 对象类型定义（#35）
□ 合并重复类型定义（#36）
□ 清理 GiftConfigModal 死代码（#37）
□ 完善 Admin 注册（#38）
□ 序列化器添加验证（#39）
□ 修复 choice 值冲突（#40）
□ 修复副作用和状态码问题（#41-42）
□ 补充单元测试（SaveProductSpecView 优先）
```

---

## 七、架构优化建议

### 7.1 后端服务层重构

当前 `SaveProductSpecView.post()` 是一个 210 行的"上帝方法"，建议拆分为：

```python
# services/product_spec_service.py
class ProductSpecService:
    @transaction.atomic
    def save_spec(self, user, data):
        submission = self._upsert_submission(user, data)
        self._sync_specs(submission, data['products'])
        self._sync_product_archives(data['products'])
        return submission

    def _upsert_submission(self, user, data): ...
    def _apply_field_mask(self, existing, new_data): ...
    def _sync_specs(self, submission, products): ...
    def _sync_product_archives(self, products): ...
    def _match_product_archive(self, product_data): ...
```

### 7.2 前端组件拆分方案

```
index.vue (3109行) →
├── useUniverSpreadsheet.ts    # Univer 初始化、单元格读写、样式管理
├── useProductForm.ts          # 表单状态、搜索自动补全、赠品/档位管理
├── useProductSpecSync.ts      # 表单 ↔ 表格双向同步
├── useProductSpecExport.ts    # CSV 导出、剪贴板复制
├── ProductSpecFormDialog.vue  # 表单对话框（从 index.vue 提取）
├── ProductSearchDropdown.vue  # 搜索下拉（从 index.vue 提取）
└── index.vue                  # 主页面编排（~300行）
```

### 7.3 字段遮罩机制统一

```python
# 当前：两套遮罩机制
# 1. LoadProductSpecView: 手动遍历 snapshot_data 的 cellData
# 2. FieldPermissionMixin: 框架层自动处理序列化器字段

# 建议：统一为序列化器层处理
class ProductSpecSubmissionSerializer:
    def to_representation(self, instance):
        data = super().to_representation(instance)
        if 'snapshot_data' in data and data['snapshot_data']:
            data['snapshot_data'] = self._apply_field_mask(
                data['snapshot_data'], self.context['request'].user
            )
        return data
```

### 7.4 缺失功能补充

| 功能 | 说明 | 优先级 |
|------|------|--------|
| **提交审批操作** | `@action detail=True methods=['post']` 的 `submit` 端点，含提交前校验 | 高 |
| **ProductSpec 筛选** | filter_fields=['submission', 'product', 'brand'] | 高 |
| **批量操作接口** | 批量更新状态、批量删除 | 中 |
| **操作日志** | 保存/提交/复制等关键操作记录 OperationLog | 中 |
| **数据导出** | 提报数据导出为 Excel | 低 |
| **自动化测试** | SaveProductSpecView 单元测试优先 | 高 |

---

## 八、关键文件索引

| 文件 | 行数 | 主要问题 |
|------|------|----------|
| `backend/dvadmin/design_order/models.py` | 188 | 字段类型不当、死模型、缺约束/索引 |
| `backend/dvadmin/design_order/views.py` | 468 | 无事务、N+1、权限弱、上帝方法 |
| `backend/dvadmin/design_order/serializers.py` | 35 | 无验证、最简形式 |
| `backend/dvadmin/design_order/urls.py` | 19 | 路由违反 RESTful |
| `backend/dvadmin/design_order/admin.py` | 5 | 仅注册1个模型 |
| `web/src/views/design_order/product_spec/index.vue` | 3109 | 崩溃bug、巨型组件、内存泄漏 |
| `web/src/views/design_order/product_spec/constants.ts` | 218 | 硬编码产品数据、重复常量 |
| `web/src/views/design_order/product_spec/utils.ts` | 120 | 中文匹配、可国际化 |
| `web/src/views/design_order/product_spec/types.ts` | 54 | 重复类型、字段类型不精确 |
| `web/src/views/design_order/product_spec/components/GiftConfigModal.vue` | 115 | 死代码、绑定模式不标准 |
| `web/src/views/design_order/product_spec_submission/crud.tsx` | 202 | 嵌套 Promise、硬编码中文 |

---

*本文档由 Claude AI 基于代码审查自动生成，建议按优先级逐步实施修复。*
